
HOST_URL='http://127.0.0.1:8000'

# Database connection
# DB connection dict:
# mysql:
# DB_CONNECTION='{"type": "mysql", "host": "ip address or host name", "user": "user_name", "password": "user_password", "port": 3306}'
#
# PostgreSQL:
# DB_CONNECTION='{"type": "postgres", "host": "ip address or host name", "user": "user_name", "password": "user_password", "port": 5432}'
#
# sqlite: None

# Target database and table schemas (can be multiple tables)
# If MCP settings is existing and reachable, database and table schemas are retrieved from MCP server,
# and the following two variables are ignored.
DATABASE='db/warehouse_and_retail_sales.sqlite'
DB_SCHEMA='CREATE TABLE "warehouse_and_retail_sales" ("year" int, "month" int, "supplier" text, "item_code" text, "item_description" text, "item_type" text, "retail_sales" real, "retail_transfers" real, "warehouse_sales" real, primary key("year", "month", "supplier", "item_code"))'

# Use MCP
# uncomment the following variables to enable MCP for db searching
# DB_SEARCH_MCP_NAME="db_searcher"
# DB_SEARCH_MCP_URL="http://127.0.0.1:8001/mcp"
# DB_SEARCH_MCP_TOOL="get_database"
# DB_SEARCH_MCP_TRANSPORT="streamable_http"
# DB_SEARCH_MCP_HEADER='{"Authorization": "Bearer YOUR_TOKEN", "X-Custom-Header": "your-custom-value"}'

LOG_LEVEL=INFO
